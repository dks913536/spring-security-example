spring:
  application:
    name: Spring-Security-Example

  datasource:
    url: jdbc:mysql://localhost:3306/springsecurity_db
    username: root
    password: Dks@913536
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
